{% extends "base.html.j2" -%}
{# Notebook -- feynman's original look: hero + sticky TOC + prose. The base
   skeleton's default blocks already produce this layout, so this theme adds
   nothing; it exists so every document names its theme explicitly. #}
